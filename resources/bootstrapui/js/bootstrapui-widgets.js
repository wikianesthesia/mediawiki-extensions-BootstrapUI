( function ( BootstrapUI ) {
    'use strict';

    /**
     * @class
     * @constructor
     *
     * @param config
     */
    BootstrapUI.AlertWidget = function( config ) {
        config = config || {};
    };

}( BootstrapUI ) );